import React from 'react';
import './Header.scss';
import Nav from '../Nav/Nav'

const Header = () => {
    return (
        <React.Fragment>
            <Nav/>
        </React.Fragment>
    );
}

export default Header;