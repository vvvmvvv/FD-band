import React from 'react';
import './Header.scss';

const Header = () => {
    return (
        <React.Fragment>
            <span>test</span>
        </React.Fragment>
    );
}

export default Header;