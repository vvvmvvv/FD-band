import React from 'react';
import './Team.scss';
import { Link, withRouter } from 'react-router-dom';

const Team = () => {
    return (
        <React.Fragment>
            
        </React.Fragment>
    );
}

export default withRouter(Team);